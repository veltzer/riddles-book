#!/bin/sh

# this scrip will install all the required packages that you need on
# ubuntu to compile and work with this package.

sudo apt-get install lacheck latex2html sketch texlive-latex-base texlive-binaries ghostscript qpdf sketch-doc poppler-utils luatex pgf tex4ht
