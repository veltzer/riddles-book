Converting latex to html
========================
- Is done using the latex2html tool.
- I still need to check if the result is good enough.
- Some makefile trickery was needed.

Makefile descisions:
====================
- output should be kept separate from the input since in this project there are a lot of junk files which are generated by the tools (latex generates many files, doing thumbs for pdf generates many files and the conversion to html generates tons).
