include files are "$ROOT/include/*.tex"

reasoning:
- we want them to have the .tex suffix so tat various text editors (vscode) will auto complete etc
	without the need for any special configuration.
